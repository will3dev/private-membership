{
  "name": "@zk-app/circuits",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "clean": "rimraf build",
    "ptau:fetch": "mkdir -p powersOfTau && [ -f powersOfTau/powersOfTau28.ptau ] || curl -L https://hermez.s3-eu-west-1.amazonaws.com/powersOfTau28_hez_final_22.ptau -o powersOfTau/powersOfTau28.ptau",
    "compile": "circom circuits/main.circom --r1cs --wasm --sym -o build",
    "setup:groth16": "snarkjs groth16 setup build/main.r1cs powersOfTau/powersOfTau28.ptau build/circuit_0000.zkey && snarkjs zkey contribute build/circuit_0000.zkey build/circuit_final.zkey -e=\"random\" && snarkjs zkey export verificationkey build/circuit_final.zkey build/verification_key.json",
    "setup:plonk": "snarkjs plonk setup build/main.r1cs powersOfTau/powersOfTau28.ptau build/circuit_final.zkey && snarkjs zkey export verificationkey build/circuit_final.zkey build/verification_key.json",
    "prove": "snarkjs groth16 prove build/circuit_final.zkey build/witness.wtns build/proof.json build/public.json",
    "verify": "snarkjs groth16 verify build/verification_key.json build/public.json build/proof.json",
    "verifier:sol": "snarkjs zkey export verifier build/circuit_final.zkey build/Verifier.sol",
    "test": "CIRCOM=/Users/will.waltrip/.cargo/bin/circom vitest run",
    "build": "CIRCOM=/Users/will.waltrip/.cargo/bin/circom circom circuits/main.circom --r1cs --wasm --sym --c"
  },
  "dependencies": {
    "circomlib": "^2.0.5",
    "circomlibjs": "^0.1.7",
    "snarkjs": "^0.7.3"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "circom_tester": "^0.0.24",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0",
    "zx": "^8.1.0"
  }
}
